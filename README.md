# Network-Performance-Bandwidth-Optimization

Для передавання сигналів через "Мости", краще використовувати task.wait(), а не RunService.Heartbeat

Для быльш зручноъ передачы даних краще використовувати словники, в яких кожен елемент також є словником

ВАЖЛИВО! Для роботи сигналів потрібно опублікувати плейс в роблокс(Publish to Roblox) А в налаштуваннях самого плейсу(браузер) необхідно увімкнути Enable Studio access to API services
